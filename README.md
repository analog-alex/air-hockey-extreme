# GLIDE.EXE

**Air Hockey Extreme** is a small browser arcade game built with Bun, Phaser, TypeScript, and Vite.

## Run Locally

```sh
bun install
bun run dev
```

## Build

```sh
bun run typecheck
bun run build
```

The static build is written to `dist/` and can be hosted on GitHub Pages.
